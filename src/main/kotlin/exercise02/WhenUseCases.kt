package exercise02

/**
 * Edit function 'getOutputBasedOnInputType' that will
 * return 'String' if input type is of type String,
 * return '0' as Int if input type is of type Int,
 * else return "else" (empty String if input type other)
 *
 * It should take only one argument and use when expression to return correct values.
 * Using if else statement is cheating :)
*/

fun getOutputBasedOnInputType(editMe: Any) = ""
